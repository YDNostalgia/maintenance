<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxa.mapper.DesaveMapper">

    <resultMap id="desaveResultMap" type="desave">
        <id property="id" column="id"></id>
        <result property="saveArea" column="saveArea"></result>
    </resultMap>
    <select id="querySave" resultMap="desaveResultMap">
        SELECT * FROM d_save
    </select>
</mapper>